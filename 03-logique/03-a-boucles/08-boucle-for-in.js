// 1)
//     Affiche les indices de chaque élément d’un tableau.

//     const couleurs = ['rouge', 'vert', 'bleu'];


const couleurs = ['rouge', 'vert', 'bleu'];

for ( index in couleurs) {
    console.log(index);
    console.log(couleurs[index]);
    
}
